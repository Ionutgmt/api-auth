# api-auth

API REST d’authentification développée avec Node.js et MongoDB.

Ce projet permet de gérer l’inscription, la connexion et l’accès à des routes protégées via un système d’authentification.

---

## Installation

Cloner:

```bash
git clone https://github.com/ionutgmt/api-auth
cd api-auth
npm install
